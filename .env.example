# Copy this to .env for local development
# Railway sets DATABASE_URL automatically in production

# Leave blank to use local SQLite (easiest for dev)
DATABASE_URL=sqlite:///./sidequests.db

# For Postgres (optional, production-like local setup):
# DATABASE_URL=postgresql://user:password@localhost:5432/sidequests
